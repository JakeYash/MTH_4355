This is a container for the class MTH 4355, Operating Systems.
Ubuntu with all the proper tools installed.
To build the first time: `docker build -t 4355-ubuntu-env .`
To start: `docker run -it -v "$(pwd):/workspace" 4355-ubuntu-env`
To exit: `exit`
Make sure docker desktop open.
